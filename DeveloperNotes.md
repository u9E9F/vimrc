# DeveloperNotes

```
so $MYVIMRC

>>> vim-obsession
Use :Obsess (with optional file/directory name) to start recording to a session
file and :Obsess! to stop and throw it away. That's it. Load a session in the usual manner: vim -S, or :source it.

```
